// This is an enum
public enum DaysOfTheWeek {
    // When we use enums and what they are?

    //enum is short for enumeration

    //enum is used when you have predefined set of values that don't change

    sunday, monday, tuesday, wednesday, thursday, friday, saturday
    
}
